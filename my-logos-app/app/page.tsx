import StudyView from '../../components/StudyView'; // Adjust path if necessary
// Import the Global CSS here (e.g., import '../styles/globals.css';)

export default function Home() {
  return (
    // The main app container (will contain your dark background CSS)
    <div className="app-container"> 
        <StudyView />
    </div>
  );
}